name: Setup Bun App
description: Checkout, setup Bun and install dependencies

runs:
  using: "composite"
  steps:
    - name: Setup Bun
      uses: oven-sh/setup-bun@v2
      with:
        bun-version: "latest"

    - name: Install deps
      shell: bash
      run: bun install
